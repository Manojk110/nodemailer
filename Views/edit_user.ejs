<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit User</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <style>
        /* Container styling for the form */
        .edit-user-container {
            max-width: 600px;
            margin: 0 auto;
            padding: 20px;
            border: 1px solid #ddd;
            border-radius: 8px;
            background-color: #f9f9f9;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        }

        /* Custom form styling */
        .custom-form {
            background-color: #fff;
            padding: 20px;
            border-radius: 8px;
            border: 1px solid #ddd;
        }

        /* Custom input field styling */
        .custom-input {
            border-radius: 4px;
            padding: 10px;
        }

        /* Custom file input styling */
        .custom-file-input {
            border-radius: 4px;
            padding: 5px;
        }

        /* Custom button styling */
        .custom-button {
            margin-top: 20px;
        }

        /* Styling for images within the form */
        .form-group img {
            margin-top: 10px;
            border-radius: 4px;
            max-width: 100%;
            height: auto;
        }

        /* Additional spacing and alignment */
        .form-group {
            margin-bottom: 15px;
        }
    </style>
</head>
<body>
    <%- include("Layout/Header") %>
    <div class="container mt-5">
       
        <h2 class="text-center mb-4">Edit User (<%= user.name %>)</h2>
        <div class="edit-user-container">
            <form action="/update/<%= user._id %>" method="post" id="add-form" enctype="multipart/form-data" class="custom-form">
                <div class="form-group">
                    <label for="name">Name</label>
                    <input type="text" name="name" class="form-control custom-input" id="name" value="<%= user.name %>" placeholder="Enter name" required>
                </div>
                <div class="form-group">
                    <label for="email">Email</label>
                    <input type="email" value="<%= user.email %>" name="email" class="form-control custom-input" id="email" placeholder="Enter email" required>
                </div>
                <div class="form-group">
                    <label for="contact">Contact</label>
                    <input type="tel" value="<%= user.contact %>" name="contact" class="form-control custom-input" id="phone" placeholder="Enter contact" required>
                </div>
                <div class="form-group">
                    <label for="image">Select Image</label>
                    <input type="file" name="image" class="form-control-file custom-file-input" id="image">
                    <img src="<%= user.image %>" width="100" class="img-thumbnail">
                </div>
                <input type="hidden" name="old_image" value="<%= user.image %>" class="btn">
                <button type="submit" class="btn btn-success btn-lg custom-button">Update User</button>
            </form>
        </div>
    </div>
</body>
</html>
